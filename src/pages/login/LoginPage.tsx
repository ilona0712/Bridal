export default function LoginPage() {
    return (
        <main style={{ padding: 24}}>
            <h1>LoginPage is wired</h1>
            <p>Next we'll recreate the Figma layout.</p>
        </main>
    );
}